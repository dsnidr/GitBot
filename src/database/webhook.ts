export const createWebhook = (
	guildId: string,
	channelId: string,
	repoUrl: string,
	secret: string
) => {
	// Placeholder
};

export const getWebhook = (guildId: string, channelId: string) => {
	// Placeholder
};

export const deleteWebhook = (webhookId: number) => {
	// Placeholder
};
